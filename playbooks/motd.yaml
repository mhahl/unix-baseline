- name: system-baseline | deploy motd
  tags:
    - motd
    - motd:configure
  with_items:
    - /etc/motd
    - /var/run/motd
  copy:
    dest: "{{item}}"
    content: |+
      $
                      __
                     / _)
            _.----._/ /
           /         /
        __/ (  | (  | It's a UNIX System!
       /__.-'|_|--|_|    I know this!