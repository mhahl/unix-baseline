- name: sigaint-soe | baseline | deploy file templates
  ansible.builtin.template:
    src: "../templates/{{ item.template }}"
    dest: "{{ item.dest }}"
    mode: "{{ item.mode }}"
  with_items:
    - { template: cron/baseline.openbsd, dest: /var/cron/tabs/baseline, mode: "0644" }