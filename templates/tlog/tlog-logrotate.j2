/var/log/tlog-rec-session.log {
  weekly
  rotate 12
  compress
  delaycompress
  missingok
  notifempty
  create 0660 tlog root
  sharedscripts
  postrotate
      # No need to signal anything - each file is a complete session
  endscript
}
